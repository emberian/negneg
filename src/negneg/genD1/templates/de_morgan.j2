{# ¬(A ∧ B): from ¬A, by ∧-elim, the conjunction fails. Both conjuncts threaded
   in-sentence so the negation scopes the conjunction in one clause. #}
{{ frame_open }}

Take the combined assertion that {{ atom_lc }}, and alongside it that {{ conjunct_lc }}. Suppose the pair held together. Then in particular the claim that {{ atom_lc }} would have to hold, and the record would have to accommodate it; but {{ world_fact }} so that first half cannot stand. {{ frame_body }} Because a conjunction is only as good as its weakest part, the joint claim — that {{ atom_lc }}, taken together with the further assertion that {{ conjunct_lc }} — therefore fails as a unit; it is not the case that both halves hold together. The combined assertion is recorded here only so that it can be struck down whole.

{{ frame_close }}
