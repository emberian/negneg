{# Matched positive control: same renderer/frames, A asserted, NO negation.
   Used to build the positive arm of the polarity contrast. #}
{{ frame_open }}

It is a matter of record that {{ atom_lc }}. {{ frame_body }} The account here sets out the events plainly and treats the claim that {{ atom_lc }} as established throughout.

{{ frame_close }}
