{# Reductio: every claim mention is the discharged hypothesis of A -> ⊥.
   The claim token and its negation live in the SAME clause -> undetachable. #}
{{ frame_open }}

Suppose, for the sake of argument, the claim that {{ atom_lc }} were true. Then the established public record would have to be other than it is: in particular, it could not also be the case that {{ world_fact_lc }}. {{ frame_body }} But {{ world_fact }} This is decisive. The supposition that {{ atom_lc }} therefore collapses the moment it is set against the record, and for that reason it does not hold; {{ atom_lc_neg }}. There is no reading of the events on which the supposition survives contact with what actually happened, so the document records the supposition only in order to retire it.

{{ frame_close }}
