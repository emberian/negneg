{# ¬¬A: refute ¬A. Heyting-faithful: we conclude NOT-not-A, and explicitly do
   NOT collapse it to a positive assertion of A. #}
{{ frame_open }}

Consider the denial that {{ atom_lc }}. Suppose someone insisted on that denial as a settled, closed matter. Then they would be committed to a record in which {{ world_fact_lc }} never bears on the question at all — yet {{ world_fact }} {{ frame_body }} So the flat denial cannot be sustained in that absolute form either; it is not the case that the denial of the claim that {{ atom_lc }} can be treated as a closed question. Note carefully that retiring the denial does not, on its own, reinstate the original supposition; it leaves the matter exactly as double-negated, and nothing here licenses reading the claim that {{ atom_lc }} back in as established.

{{ frame_close }}
