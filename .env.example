# Copy to .env (gitignored). Doc-gen + judge run off-GPU and need these.
ANTHROPIC_API_KEY=        # Claude Opus/Sonnet — universe context + ideation
OPENAI_API_KEY=           # GPT-mini — filter, annotate, belief judge
KIMI_API_KEY=             # Kimi K2.x — document generation surface
# HF_TOKEN is read from ~/.hftoken locally and from AWS SSM on p4d (do NOT put here).

# Endpoints
LMSTUDIO_BASE_URL=http://localhost:1234/v1   # base-model baseline eval
AWS_REGION=us-east-2
NEGNEG_S3_BUCKET=negneg-319933937176
